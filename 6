Drone.io, Gitea, NFS, docker...

NFS share on synology
Docker volume on NFS share
Docker-compose for drone, gitea and postgresql
  Mariadb/mysql didn't work due to using the same nfs share
Docker registry
  Docker needs "insecure-registries" flag
  Docker snap on ubuntu isn't configured as regular docker
  https://github.com/docker/docker-snap/issues/22
  
